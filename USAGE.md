# 사용 설명서 (USAGE)

> 이 문서는 **코딩이나 터미널을 전혀 몰라도** 이 프로그램으로 나만의 블로그를 만들 수 있도록
> 아주 쉽게 설명한 가이드입니다. (개발자용 기술 설계 문서는 `README.md`를 참고하세요.)

---

## 🌟 이 프로그램은 무엇인가요?

**메모장에 글만 쓰면, 예쁜 나만의 블로그 홈페이지로 뚝딱 변신시켜주는 마법 상자예요.** ✨

어려운 홈페이지 제작 툴을 몰라도, 그냥 `.md`(마크다운)라는 간단한 텍스트 파일로 글을 쓰면
프로그램이 알아서 보기 좋은 HTML 웹페이지로 만들어줍니다.

---

## 📦 실행을 위한 준비물

시작하기 전에 딱 하나만 설치하면 됩니다: **Node.js**

1. 아래 공식 사이트에 접속하세요.
   👉 https://nodejs.org
2. 화면에 보이는 두 개의 다운로드 버튼 중 **"LTS"** 라고 적힌 왼쪽 버튼을 눌러 설치 파일을 받으세요.
   (LTS = 가장 안정적인 버전이라는 뜻이에요. 무조건 이걸 받으면 됩니다.)
3. 다운로드된 설치 파일을 실행하고, **"Next(다음)"** 버튼만 계속 눌러서 설치를 끝내세요.
4. 설치가 끝나면 컴퓨터를 한 번 재시작해주세요.

이제 준비 끝! 컴퓨터에 있는 **"터미널"** 또는 **"명령 프롬프트"** 라는 검은 화면 프로그램만 열 줄 알면 됩니다.
(윈도우는 시작 메뉴에서 "cmd" 검색, 맥은 "터미널" 검색)

터미널을 열었다면, 이 프로젝트 폴더(`orchestration_test`)로 이동해서 아래 명령을 **딱 한 번만** 실행해주세요.
(이미 실행했다면 건너뛰어도 돼요.)

```bash
npm install
```

화면에 알 수 없는 글자들이 좌라락 지나가면 정상입니다. 잠시 기다리면 끝나요.

---

## ✍️ 나만의 새 글 추가하는 방법

1. 이 프로젝트 폴더 안에 있는 **`posts`** 폴더를 여세요.
2. 그 안에 있는 아무 파일(예: `hello-world.md`)을 하나 복사해서, 이름을 원하는 대로 바꾸세요.
   - 예: `나의-첫번째-일기.md`
   - ⚠️ 파일 이름은 꼭 **영어, 숫자, 하이픈(-)** 으로만 짓는 걸 추천해요 (한글도 되지만 안전하게!).
   - ⚠️ 파일 끝은 반드시 **`.md`** 로 끝나야 해요.
3. 메모장(또는 VS Code 같은 편집기)으로 그 파일을 열어서, 아래 형식대로 맨 위에 **"글 정보(Frontmatter)"** 를 적어주세요.

```markdown
---
title: 나의 첫번째 일기
date: 2026-09-04
---

# 오늘 있었던 일

여기서부터 자유롭게 글을 쓰면 됩니다!

- 이렇게 목록도 쓸 수 있고
- **굵은 글씨**나 *기울임 글씨*도 가능해요

> 인용문도 쓸 수 있어요.
```

**꼭 기억할 규칙 3가지:**

| 항목 | 설명 |
|------|------|
| 맨 위 `---` `---` | 이 사이에는 반드시 `title`(글 제목)과 `date`(날짜, `YYYY-MM-DD` 형식)를 적어주세요 |
| `title` | 여러분 글의 제목이에요. 목록 페이지와 글 페이지 상단에 표시됩니다 |
| `date` | `2026-09-04` 처럼 연도-월-일 형식으로 적어주세요. 최신 글이 목록 맨 위에 오게 됩니다 |

글 내용은 그 아래에 자유롭게 마크다운 문법(`#` 제목, `-` 목록, `**굵게**` 등)으로 작성하면 됩니다.

파일을 저장했다면 끝! 이제 빌드만 하면 됩니다.

---

## 🚀 단 한 줄로 블로그 빌드하고 결과물 확인하는 방법

터미널에서 프로젝트 폴더로 이동한 뒤, 아래 명령어 **딱 한 줄**만 입력하고 엔터를 누르세요.

```bash
npm run build
```

이 한 줄이 여러분이 `posts` 폴더에 써놓은 모든 글을 읽어서, 예쁜 HTML 홈페이지로 통째로 만들어줍니다.

터미널에 아래처럼 나오면 성공이에요:

```
[build] 3개의 포스트를 처리했습니다.
  - .../dist/posts/나의-첫번째-일기.html
  - .../dist/index.html
```

### 결과물 확인하기

1. 프로젝트 폴더 안에 새로 생긴 **`dist`** 폴더를 여세요.
2. 그 안의 **`index.html`** 파일을 더블클릭하세요. (또는 우클릭 → "연결 프로그램" → 크롬/엣지 등 웹 브라우저 선택)
3. 짜잔! 여러분의 블로그 홈페이지가 브라우저에 열립니다 🎉
4. 글 제목을 클릭하면 각 글의 상세 페이지(`dist/posts/*.html`)로 이동합니다.

> 💡 새 글을 추가하거나 기존 글을 수정했다면, `npm run build`를 다시 한 번 실행하고
> 브라우저를 새로고침(F5)하면 최신 내용으로 반영됩니다.

축하합니다, 이제 여러분만의 블로그가 완성되었습니다! 🎊
